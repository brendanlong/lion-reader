<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
        Base theme for Lion Reader.
        Compose handles theming via LionReaderTheme, but we need a base
        theme for the Activity splash screen and system UI styling.
    -->
    <style name="Theme.LionReader" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Status bar color -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Navigation bar color -->
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <!-- Enable edge-to-edge -->
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <!--
        Translucent theme for share receiver activity.
        Shows a dialog-style overlay while saving articles.
    -->
    <style name="Theme.LionReader.Translucent" parent="android:Theme.Material.Light.Dialog">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
