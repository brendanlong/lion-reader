# Discord application credentials
# Use the SAME Discord application as Lion Reader's OAuth integration
# so the bot and "Sign in with Discord" appear as the same app to users.
#
# DISCORD_CLIENT_ID should match the one in Lion Reader's .env
# DISCORD_TOKEN is the bot token (Bot > Reset Token in Discord Developer Portal)
DISCORD_CLIENT_ID=your_client_id_here
DISCORD_TOKEN=your_bot_token_here

# Lion Reader API URL (defaults to https://lionreader.app)
LION_READER_URL=https://lionreader.app

# Emoji to trigger saving (defaults to lion emoji)
SAVE_EMOJI=🦁
