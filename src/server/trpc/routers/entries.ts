/**
 * Entries Router
 *
 * Handles entry listing and actions: list, get, mark read, star.
 * Will be implemented in Phase 5.
 */

import { createTRPCRouter } from "../trpc";

export const entriesRouter = createTRPCRouter({
  // Placeholder - will be implemented in Phase 5
  // list: protectedProcedure.input(...).query(...)
  // get: protectedProcedure.input(...).query(...)
  // markRead: protectedProcedure.input(...).mutation(...)
  // markAllRead: protectedProcedure.input(...).mutation(...)
  // star: protectedProcedure.input(...).mutation(...)
  // unstar: protectedProcedure.input(...).mutation(...)
});
