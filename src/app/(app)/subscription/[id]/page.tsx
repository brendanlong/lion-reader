/**
 * Single Subscription Page
 *
 * Server component that renders the single subscription view.
 */

import { SingleSubscriptionClient } from "./client";

export default function SingleSubscriptionPage() {
  return <SingleSubscriptionClient />;
}
