/**
 * Saved Articles Page
 *
 * Server component that renders the saved articles view.
 */

import { SavedArticlesClient } from "./client";

export default function SavedArticlesPage() {
  return <SavedArticlesClient />;
}
