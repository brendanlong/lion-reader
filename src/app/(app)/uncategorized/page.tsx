/**
 * Uncategorized Entries Page
 *
 * Server component that renders the uncategorized entries view.
 */

import { UncategorizedEntriesClient } from "./client";

export default function UncategorizedEntriesPage() {
  return <UncategorizedEntriesClient />;
}
